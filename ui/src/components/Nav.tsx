import { FC } from "react"
import Button from "./ui/Button"

const Nav: FC = () => {
  return (
    <nav>
      <Button>Sign Up</Button>
      <Button>Sign In</Button>
    </nav>
  )
}

export default Nav
